
  # main project

  This is a code bundle for main project. The original project is available at https://www.figma.com/design/VuPTKnfCGY1tbP73IYDCaA/main-project.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  